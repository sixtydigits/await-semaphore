{
  "name": "await-semaphore",
  "version": "0.1.4",
  "description": "Awaitable semaphore/mutex",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "typings install",
    "build": "rm -rf dist && tsc",
    "test": "rm -rf dist && tsc && mocha ./dist/test.js",
				"clean": "rm -rf dist"
		},
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sixtydigits/await-semaphore.git"
  },
  "keywords": [
    "promise",
    "semaphore",
    "mutex",
    "typescript"
  ],
  "author": "Emma Kuo (original)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/sixtydigits/await-semaphore/issues"
  },
  "homepage": "https://github.com/sixtydigits/await-semaphore#readme",
  "devDependencies": {
    "mocha": "^6.0.2",
    "typescript": "^3.3.3333",
    "typings": "^2.1.1"
  }
}
